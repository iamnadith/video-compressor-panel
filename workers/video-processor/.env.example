# The processor talks only to the backend orchestrator.
# It never receives panel, PostgreSQL, database, or R2 credentials.
ORCHESTRATOR_URL=https://your-orchestrator.workers.dev
WORKER_SHARED_SECRET=replace-with-the-secret-saved-in-dashboard-settings

# FFmpeg and FFprobe must be installed separately and available on PATH.
